# Feedback

## 用户反馈（2026-04-04）

1. **去除左侧栏消息通知功能**：侧边栏的"通知"tab（NotificationPanel）去掉，包括 NavRail 中的通知铃铛图标和通知面板入口。通知功能不需要。

2. **工作流 UI 优化（P1）**：点击工作流后主界面只显示文字信息，没有画布视图/编辑功能。需要：点击工作流后主界面切换为完整的工作流编辑器视图（包含画布），而不只是静态信息展示。

3. **去除启动 Loading 界面**：打开 AIPA 程序时显示的 Loading... 启动画面（splash screen）要去掉，直接显示应用主界面。

4. **Qwen 模型授权方式重构（P1）**：当前 Qwen 接入方式是扫二维码后手动填写 API key，不合理。应改为：通过千问 App 的扫描功能直接 OAuth 授权登录，授权后自动获取 token 使用 Qwen 服务，无需手动复制粘贴 key。
