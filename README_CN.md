<p align="center">
  <h1 align="center">AIPA — AI 个人助手</h1>
  <p align="center">
    你的全天候桌面智能伙伴。<br/>
    随问随答，随需随做，事事搞定。
  </p>
  <p align="center">
    <img src="https://img.shields.io/badge/平台-Windows-blue" alt="平台" />
    <img src="https://img.shields.io/badge/Electron-39-47848F" alt="Electron" />
    <img src="https://img.shields.io/badge/React-18-61DAFB" alt="React" />
    <img src="https://img.shields.io/badge/TypeScript-strict-3178c6" alt="TypeScript" />
    <img src="https://img.shields.io/badge/i18n-EN%20%7C%20%E4%B8%AD%E6%96%87-brightgreen" alt="i18n" />
  </p>
</p>

---

AIPA 是一款基于 Electron + React 构建的桌面 AI 个人助手。它不仅仅是一个聊天窗口——而是一个真正驻留在你桌面上的智能代理：它能理解你的文件与系统环境，编写并执行代码，浏览网页，从头到尾地完成现实世界中的任务。底层通过驱动 [Claude Code](https://claude.ai/code) CLI 作为执行引擎，同时支持 OpenAI、DeepSeek、Ollama 以及任何 OpenAI 兼容的提供商。

## 产品愿景

大多数 AI 工具是"回答机器"。AIPA 要做的是一个**行动者**。

- 你描述你要做什么——它负责想清楚怎么做。
- 它可以读取、创建、修改你本地的任何文件。
- 它可以运行 Shell 命令和脚本。
- 它可以浏览网页、抓取信息。
- 它记住你的历史会话，随时继续上次的工作。
- 它将每一步操作透明呈现，你始终掌握最终决定权。

**Claude Code CLI 是引擎，AIPA 是驾驶舱。**

---

## 功能概览

### 对话与聊天

| 功能 | 说明 |
|------|------|
| **对话面板** | 通过 stream-JSON 协议进行结构化对话，支持工具调用可视化 |
| **终端面板** | 基于 xterm.js 的完整交互式 PTY 终端，原生 PTY 不可用时自动切换到基本 Shell 模式 |
| **消息编辑** | 编辑任意已发送消息，对话从该处截断并重新发送以获取新回复 |
| **重新生成回复** | `Ctrl+Shift+R` 重新生成；拆分按钮可先选择不同模型再重新生成 |
| **跟进建议** | 每次回复后自动显示 2-3 个上下文相关的跟进建议（如"解释这段代码"、"关键要点"） |
| **思考块** | 可折叠的扩展思考过程展示，流式传输时自动展开 |
| **消息朗读** | Web Speech API 朗读回复；自动去除 Markdown 格式 |
| **引用回复** | 选中文本后以 `>` Markdown 格式引用到下一条消息中 |
| **权限提示** | 友好的允许/拒绝卡片显示工具权限请求（Bash、写文件、编辑、网页访问等） |
| **计划卡片** | 在对话中以结构化卡片展示执行计划 |

### 输入与编写

| 功能 | 说明 |
|------|------|
| **Slash 命令** | 输入 `/` 弹出可用命令列表 |
| **@提及弹窗** | 通过 `@` 引用文件和上下文 |
| **输入自动补全** | 根据历史记录在光标后显示灰色建议文本，按 Tab 接受 |
| **文本片段** | `::关键词` 触发器展开可重用的文本片段 |
| **文本变换** | 通过魔棒按钮将文本变为正式、随意、更短、更长或修正语法 |
| **回复风格选择器** | 选择回复风格（简洁、详细、专业、随意、创意），修改 AI 回复风格 |
| **内联计算器** | 输入 `= 42 * 1.18` 即可看到即时计算结果；按 Tab 采用 |
| **日期/时间插入** | 日历按钮提供 7 种本地化日期/时间格式 |
| **Markdown 快捷键** | `Ctrl+B` 加粗、`Ctrl+I` 斜体、`Ctrl+Shift+U` 大小写循环 |
| **剪贴板快捷操作** | 粘贴并提问：摘要、翻译、改写、解释、语法检查 |
| **URL 粘贴检测** | 粘贴 URL 后显示快捷操作按钮（摘要、解释、翻译） |
| **长文本粘贴检测** | 粘贴 500+ 字符显示操作按钮；12 秒后自动消失 |
| **输入历史** | 上/下方向键在历史发送记录中切换；跨重启持久保存 |
| **任务队列** | 排队多条指令依次自动执行；支持暂停、恢复或清空 |
| **图片粘贴与拖放** | 直接将图片粘贴或拖放文件到对话输入框 |
| **输入进度环** | 发送按钮外围 SVG 进度环随输入长度填充，颜色编码 |

### 会话管理

| 功能 | 说明 |
|------|------|
| **历史会话** | 浏览和恢复历史对话，支持搜索高亮和骨架屏加载 |
| **跨会话搜索** | `Ctrl+Shift+F` 或在搜索栏按回车搜索所有会话文件 |
| **会话标签** | 6 种彩色标签（工作、个人、研究、调试、文档、归档），支持筛选 |
| **会话置顶** | 星标会话快速访问；置顶会话排在最前面 |
| **项目筛选** | 使用标签式按钮按项目路径筛选会话 |
| **批量删除** | 选择多个会话一次性删除，带确认提示 |
| **自动标题** | 首次对话后自动生成会话标题 |
| **会话导出** | 导出为 Markdown、HTML 或 JSON；HTML 生成自包含的样式化文件 |
| **继续上次对话** | 欢迎页面一键恢复最近的会话 |
| **排序选项** | 按最新、最旧、字母序或消息数量排序 |

### 笔记系统

| 功能 | 说明 |
|------|------|
| **快捷笔记** | 侧边栏内置笔记本；自动保存，最多 100 条 |
| **笔记分类** | 最多 10 个彩色分类标签，支持筛选 |
| **Markdown 预览** | 在原始编辑和渲染预览之间切换 |
| **格式化工具栏** | 加粗、斜体、标题、列表、代码、链接按钮 |
| **模板** | 会议记录、待办事项、日记、灵感记录 |
| **笔记置顶与排序** | 置顶笔记到顶部；按修改时间、创建时间或字母序排序 |
| **搜索与高亮** | 全文搜索，匹配内容高亮显示 |
| **导出与导入** | 单个或批量导出为 Markdown；导入 .md/.txt 文件 |
| **从聊天保存** | 一键将输入文本或助手回复保存为笔记 |

### AI 角色

| 功能 | 说明 |
|------|------|
| **自定义角色** | 最多 10 个角色，每个有名称、Emoji、模型、系统提示词和标记颜色 |
| **快速切换** | 从聊天顶栏、状态栏或命令面板切换角色 |
| **角色头像** | 消息显示角色 Emoji；思考指示器显示角色名称 |
| **角色建议卡** | 欢迎页面显示角色专属的建议卡片 |
| **预设角色** | 助手、写手、分析师、导师、程序员——一键安装 |
| **导出与导入** | 以 JSON 文件分享角色配置 |

### 记忆与工作流

| 功能 | 说明 |
|------|------|
| **持久化记忆** | 大脑图标（`Ctrl+6`）：4 种分类（偏好、事实、指令、上下文），最多 200 条 |
| **记忆注入** | 置顶记忆 + 最近 10 条记忆自动注入每次对话 |
| **记住此内容** | 从悬停工具栏一键将回复保存为上下文记忆 |
| **工作流构建器** | 工作流图标（`Ctrl+7`）：将多个提示词串联成可重复使用的流水线；内含定时提示标签页 |
| **定时提示** | 已内置于工作流面板——每日、每周、每月定时自动执行 |
| **提示历史** | 列表重播图标（`Ctrl+8`）：可搜索的历史记录，带频率追踪和收藏 |

### 多模型支持

| 功能 | 说明 |
|------|------|
| **提供商管理** | 配置 Claude、OpenAI、DeepSeek、Ollama 和自定义 OpenAI 兼容端点 |
| **模型选择器** | 按提供商分组，带能力标签（视觉、代码、推理）和健康状态指示点 |
| **自动故障转移** | 提供商故障时自动切换到故障转移链中的下一个，Toast 通知提示 |
| **API 密钥池** | 多个 API 密钥，配额耗尽时自动轮换 |

### 技能市场

| 功能 | 说明 |
|------|------|
| **技能浏览器** | 浏览已安装的技能（个人级 + 项目级），支持搜索和详情查看 |
| **精选市场** | 来自 Anthropic、OpenClaw、ClawhHub 和社区贡献者的 46+ 个技能 |
| **来源筛选** | 按来源（Anthropic/OpenClaw/ClawhHub/社区）和 7 个分类筛选 |
| **一键安装** | 安装技能，标注作者和来源链接 |
| **ClawhHub 集成** | 从 ClawhHub.ai API 实时获取技能 |

### 外观与主题

| 功能 | 说明 |
|------|------|
| **深色、浅色和跟随系统主题** | 跟随系统模式实时响应 OS 偏好；`Ctrl+Shift+D` 循环切换 |
| **聊天缩放** | `Ctrl+=` 放大、`Ctrl+-` 缩小、`Ctrl+0` 重置（70%-150% 范围） |
| **紧凑模式** | 通过设置启用更紧凑的消息布局 |
| **专注模式** | `Ctrl+Shift+F` 隐藏侧边栏和终端 |
| **消息动画** | 方向感知的滑入动画，带发光效果；尊重 `prefers-reduced-motion` |
| **可展开导航栏** | 在纯图标和图标+标签的侧边栏导航之间切换 |

### 键盘快捷键

| 快捷键 | 操作 |
|--------|------|
| `Ctrl+N` | 新建对话 |
| `Ctrl+F` | 对话内搜索 |
| `Ctrl+Shift+F` | 全局跨会话搜索 |
| `Ctrl+Shift+P` | 命令面板 |
| `Ctrl+Shift+E` | 导出对话 |
| `Ctrl+Shift+D` | 切换主题 |
| `Ctrl+Shift+L` | 切换语言 |
| `Ctrl+Shift+R` | 重新生成回复 |
| `Ctrl+Home/End` | 跳转到第一条/最后一条消息 |
| `Alt+Up/Down` | 在用户消息之间跳转 |
| `Ctrl+1-5` | 切换侧边栏标签页 |
| `Ctrl+6-8` | 记忆、工作流、提示历史 |
| `Ctrl+/` | 快捷键速查表 |
| `Escape` | 停止流式传输 |

### 无障碍与国际化

| 功能 | 说明 |
|------|------|
| **多语言** | 英文和简体中文；默认跟随系统语言 |
| **ARIA 地标** | `role="application"`、`role="navigation"`、`aria-live` 区域、所有交互元素有 `aria-label` |
| **焦点陷阱** | 模态对话框（命令面板、图片灯箱）限制 Tab 导航 |
| **TypeScript 零错误** | 严格模式下 `tsc --noEmit` 零编译错误 |

### 状态栏

| 功能 | 说明 |
|------|------|
| **Token 用量与费用** | 实时上下文用量条、每轮 Token 计数、累计费用——点击可复制 |
| **流式速度** | 活跃流式传输时显示每秒字符数和预估 Token/秒 |
| **模型切换器** | 点击模型徽章打开快捷切换下拉框 |
| **角色切换器** | 从状态栏快速切换角色 |
| **专注计时器** | 25 分钟番茄钟倒计时，完成时弹出通知 |
| **秒表** | 正计时秒表；点击开始/暂停，双击重置 |

---

## 安全

- **safeStorage 加密** -- API 密钥通过操作系统密钥链加密存储（Windows 使用 DPAPI，macOS 使用 Keychain），通过 `electron.safeStorage` 实现
- **内容安全策略** -- 严格的 CSP 头部防止 XSS 和未授权的外部资源加载
- **沙箱化渲染进程** -- 启用 `sandbox: true`；渲染进程无法直接访问 Node.js
- **IPC 输入验证** -- 所有文件系统 IPC 处理器对路径进行白名单验证
- **环境变量净化** -- CLI 子进程仅接收明确白名单中的环境变量

---

## 环境要求

- Windows 10/11 x64
- [Node.js](https://nodejs.org/) 18 或更高版本（需在 PATH 中）
- [Anthropic API 密钥](https://console.anthropic.com/)（用于 Claude 模型）
- 可选：OpenAI API 密钥、DeepSeek API 密钥，或本地 [Ollama](https://ollama.ai/) 实例（用于多模型支持）

## 快速开始

```bash
# 1. 克隆仓库
git clone https://github.com/CipherSlinger/AIPA.git
cd AIPA/electron-ui

# 2. 安装依赖
npm install

# 3. 构建
npm run build

# 4. 启动
node_modules/.bin/electron dist/main/index.js
```

首次启动后，点击侧边栏齿轮图标进入**设置**，填入你的 Anthropic API 密钥。

## 开发模式（热更新）

```bash
# 终端 1 — 构建主进程 & 启动 Vite 开发服务器
npm run build:main && npm run build:preload
npx vite

# 终端 2 — 以开发模式启动 Electron
NODE_ENV=development node_modules/.bin/electron dist/main/index.js
```

## 打包安装包（Windows）

```bash
npm run dist:win
# 输出目录：electron-ui/release/
```

## 更新内置 CLI

`package/` 目录中包含内置的 Claude Code CLI。项目提供了两个辅助脚本，用于检测并更新它：

```bash
# Bash（Linux / macOS / Windows Git Bash）
bash update-cli.sh

# Windows 命令提示符
update-cli.bat
```

两个脚本均会将 `package/package.json` 中的当前版本与 npm 上的最新版本进行比对，并在下载替换 `package/` 前提示你确认。

## 项目结构

```
AIPA/
  package/          # 内置的 Claude Code CLI（只读，已打包）
  electron-ui/      # Electron 应用
    src/
      main/         # 主进程（Node.js）：PTY、IPC、会话、配置、提供商
      preload/      # 上下文桥接（将 electronAPI 暴露给渲染进程）
      renderer/     # React 界面（Vite + Zustand + i18n）
    dist/           # 编译产物（构建生成，不提交到 git）
```

应用通过两种模式与 Claude Code CLI 交互：

| 模式 | 用途 | 原理 |
|------|------|------|
| **PTY**（node-pty） | 终端面板 | 通过 ConPTY 启动 `node cli.js`，将原始终端 I/O 流传输给 xterm.js |
| **Stream-JSON** | 对话面板 | 启动 `node cli.js --input-format stream-json --output-format stream-json --print`，解析 NDJSON 事件流 |

渲染进程与主进程之间的所有通信通过 `window.electronAPI` 进行（定义于 `src/preload/index.ts`）。

## 许可证

MIT
