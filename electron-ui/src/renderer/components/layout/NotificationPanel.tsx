// NotificationPanel — removed in Iteration 468 (notifications tab deprecated)
// This file is intentionally empty. The notification panel was replaced by
// desktop notifications (OS-level) and toast notifications (in-app).
// Kept as empty module to prevent import errors in case of stale references.
export default function NotificationPanel() { return null }
